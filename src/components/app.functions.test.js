import AppFunctions from './app.functions.js';

const testMatches = {
	value: 'value',
	match: 'match'
};

describe('App functionality', () => {
  describe('newMatches function', () => {
    it('', () => {

    });
  });
});